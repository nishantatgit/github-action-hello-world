name: 'Hello World'
description: 'Greet someone and record time'
inputs:
 who-to-greet:
  description: 'who to greet'
  required: true
  default: 'World'
 inputList:
  description: 'list to login'
  required: false
outputs:
 time:
  description: 'The time we greeted you'
runs:
 using: 'node12'
 main: 'index.js'